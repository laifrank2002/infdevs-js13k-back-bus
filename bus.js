//i'm using iifes for easier compression
//unfinished

//function to create buses
var create_bus = (function() {
    var bus_speed = 1 / 30000; //it takes 30 seconds to travel 1 unit (1 city block)
    
    return function(route, x, y, direction) {
        
    };
})();

//generates the minor stops along the routes
var create_stops = (function() {
    var distance_between_stops = 0.3;
    
    return function(route) {
        
    };
})();

//gets any routes that stop at that stop
var get_routes = (function(routes) {
    return function(stop) {
        routes.forEach(r => {
            
        });
    };
})();